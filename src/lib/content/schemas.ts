import { FlagPuzzleSchema } from '$lib/model/FlagPuzzle.ts';
import { LevelPackSchema } from '$lib/model/LevelPack.ts';

export const schemas = {
  flag: FlagPuzzleSchema,
  pack: LevelPackSchema,
};
